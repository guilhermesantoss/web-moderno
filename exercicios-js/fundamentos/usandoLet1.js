let numero = 1;
{
    let numero = 2;
    console.log('dentro = ', numero);
}

console.log('fora = ', numero);

// variaveis criadas com a palavra reservada var, tem escopo global e de funcao

// as variaveis criadas com a palvra reservada let, tem escopo global, de funcao e de bloco